<div class="contents row" >
  <p><%= @user_name %>さんの投稿一覧</p>
  <% @posts.each do |post| %>
    <div class="content_post">
      <%= simple_format(tweet.text) %>
      <span class="name"><%= post.book %></span>
    </div>
  <% end %>
</div>